# Environment: dev | staging | prod
TEST_ENV=dev

# Dev credentials
DEV_USER=dev_user
DEV_PASS=dev_password

# Staging credentials
STAGING_USER=staging_user
STAGING_PASS=staging_password

# Prod credentials (use with caution)
PROD_USER=
PROD_PASS=
